# UniPrep - Приложение для подготовки к экзаменам

Приложение для подготовки к рубежкам и экзаменам по принципу ПДД-тестов.

## Возможности

- 🔐 Регистрация и авторизация пользователей
- 👤 Профиль пользователя с редактируемыми данными
- 📚 Выбор курса, предмета и типа теста (РК1, РК2, Экзамен)
- 📝 Прохождение тестов с визуальной обратной связью
- 📊 История пройденных тестов
- 🎨 Современный и удобный интерфейс

## Технологии

- **Backend**: Go (Golang)
- **Database**: PostgreSQL
- **Frontend**: HTML, CSS, JavaScript (Vanilla)
- **Routing**: Gorilla Mux
- **Sessions**: Gorilla Sessions

## Установка и запуск

### Требования

- Go 1.21+
- PostgreSQL 12+
- Git

### Настройка базы данных

1. Создайте базу данных:
```sql
CREATE DATABASE uniprep;
```

2. Обновите строку подключения в `cmd/server/main.go` или установите переменную окружения:
```bash
export DATABASE_URL="postgres://username:password@localhost/uniprep?sslmode=disable"
```

### Установка зависимостей

```bash
go mod download
```

### Запуск приложения

```bash
go run cmd/server/main.go
```

Или с параметрами:

```bash
go run cmd/server/main.go -port=8080 -db="postgres://postgres:postgres@localhost/uniprep?sslmode=disable"
```

Приложение будет доступно по адресу: `http://localhost:8080`

## Структура проекта

```
uniprep/
├── cmd/
│   └── server/
│       └── main.go          # Точка входа приложения
├── internal/
│   ├── database/
│   │   └── db.go            # Подключение к БД и миграции
│   ├── handlers/
│   │   ├── auth.go          # Обработчики аутентификации
│   │   └── tests.go         # Обработчики тестов
│   ├── middleware/
│   │   └── auth.go          # Middleware для проверки авторизации
│   └── models/
│       ├── user.go          # Модели пользователя
│       └── test.go          # Модели тестов
├── frontend/
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css    # Стили приложения
│   │   └── js/
│   │       ├── main.js      # Главная страница
│   │       ├── auth.js      # Авторизация
│   │       ├── tests.js     # Выбор тестов
│   │       ├── test.js      # Прохождение теста
│   │       └── profile.js   # Профиль
│   └── templates/
│       ├── index.html       # Главная страница
│       ├── login.html       # Страница входа
│       ├── register.html    # Страница регистрации
│       ├── tests.html       # Выбор тестов
│       ├── test.html        # Прохождение теста
│       └── profile.html     # Профиль
├── go.mod                   # Зависимости Go
└── README.md               # Документация
```

## API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация нового пользователя
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход из системы
- `GET /api/profile` - Получить профиль пользователя
- `PUT /api/profile` - Обновить профиль пользователя

### Тесты
- `GET /api/courses` - Получить список курсов
- `GET /api/courses/{courseId}/subjects` - Получить предметы курса
- `GET /api/test-types` - Получить типы тестов
- `GET /api/subjects/{subjectId}/test-types/{testTypeId}/questions` - Получить вопросы теста
- `POST /api/tests/submit` - Отправить результаты теста
- `GET /api/tests/history` - Получить историю тестов

## Особенности MVP

- Для каждого предмета создан один тестовый вопрос
- При прохождении теста этот вопрос повторяется 25 раз
- Все данные автоматически заполняются при первом запуске

## Развертывание

Для развертывания на VPS:

1. Установите зависимости
2. Настройте PostgreSQL
3. Соберите приложение: `go build -o uniprep cmd/server/main.go`
4. Запустите: `./uniprep`

Или используйте systemd для автоматического запуска.

## Лицензия

MIT
# UniPrep
